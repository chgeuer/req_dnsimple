# shellcheck shell=bash disable=SC2034
CAMPAIGN_NAME="req_dnsimple API parity"
CAMPAIGN_PROJECTS=(.)
CAMPAIGN_LABELS=(api-parity)
CAMPAIGN_PROMPT_FILE="prompt.md"
CAMPAIGN_GUARDRAILS="Work only on branch campaign/api-parity and one ready api-parity issue. Preserve existing public success shapes. Use offline Req adapters and fake credentials only; never call live DNSimple APIs, change real DNS, make purchases, read secrets, or edit the reference CLI. No pushes, destructive git operations, or Co-authored-by trailers. Do not close peers or epics, remove campaign labels, or conceal unfinished work in another status."

MAX_ITERS="${MAX_ITERS:-104}"
RUN_TIMEOUT="${RUN_TIMEOUT:-1800}"

# Leave primary model and effort selection to the configured Copilot defaults.
COPILOT_FALLBACK_MODEL=""
